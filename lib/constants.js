/**
 * The default license filename. Used when `config.licenseLink` is set as
 * `true`.
 *
 * @type {string}
 */
export const DEFAULT_LICENSE_FILENAME = 'LICENSE';

/**
 * The placeholder text for sections that have no defined body.
 *
 * @type {string}
 */
export const PLACEHOLDER = '_To be documented._';
